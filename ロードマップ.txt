# WebDM Pro Editor - 無料スタック完全実装ガイド

## 🎯 目標：既存コード + GitHub + Netlify で無料起動

---

## 📊 完全無料で実現可能な構成

```
現状: HTML単一ファイル（v1.8.5.1）
  ↓
目標: Netlify にホスティング + Stripe決済 + Supabase DB

投資: ¥0（初期段階）
```

---

## 🚀 Phase 1: GitHub へのアップロード（Week 1）

### Step 1: リポジトリ作成

```bash
# ローカルで初期化
mkdir webdm-pro
cd webdm-pro

# Git 初期化
git init
git config user.name "Your Name"
git config user.email "your.email@example.com"

# GitHub CLI でリポジトリ作成
gh repo create webdm-pro \
  --public \
  --source=. \
  --remote=origin \
  --push

# または Web で https://github.com/new から手動作成
```

### Step 2: 既存コードを GitHub に push

```bash
# 既存の HTML ファイルをプロジェクトに追加
cp /path/to/webdm-v1.8.5.1.html ./index.html

# .gitignore 作成（重要：API キーを含めない）
cat > .gitignore << 'EOF'
.env
.env.local
node_modules/
dist/
.DS_Store
*.log
EOF

# .env テンプレート作成（本人だけが秘密キーを保持）
cat > .env.example << 'EOF'
VITE_GOOGLE_API_KEY=your-api-key-here
VITE_STRIPE_PUBLIC_KEY=your-stripe-key-here
EOF

# README 作成
cat > README.md << 'EOF'
# WebDM Pro Editor

AI がメール案を自動生成する SaaS ツール

## セットアップ

```bash
npm install
npm run dev
```

## 環境変数

`.env.local` ファイルを作成して以下を設定:
- `VITE_GOOGLE_API_KEY`: Google Gemini API キー
- `VITE_STRIPE_PUBLIC_KEY`: Stripe 公開キー
EOF

# コミット
git add .
git commit -m "Initial commit: WebDM v1.8.5.1"
git push origin main
```

---

## 🔗 Phase 2: Netlify へのデプロイ（Week 2）

### Step 1: Netlify アカウント作成＆接続

```bash
# Netlify CLI インストール
npm install -g netlify-cli

# Netlify アカウント作成（無料）
# https://app.netlify.com/signup

# GitHub 認証
netlify login

# サイト初期化
netlify init

# 質問への回答:
# - Site name: webdm-pro
# - Build command: (なし - そのまま Enter)
# - Publish directory: . (カレントディレクトリ)
```

### Step 2: netlify.toml 作成（自動デプロイ設定）

```toml
# netlify.toml

[build]
publish = "."
command = ""

[dev]
command = "npx http-server"
port = 3000

[[redirects]]
from = "/*"
to = "/index.html"
status = 200

# 環境変数の設定
[context.production.environment]
VITE_GOOGLE_API_KEY = "@vite_google_api_key"
VITE_STRIPE_PUBLIC_KEY = "@vite_stripe_public_key"
```

### Step 3: 環境変数を Netlify で設定

```bash
# コマンドラインで設定
netlify env:set VITE_GOOGLE_API_KEY "your-gemini-api-key"
netlify env:set VITE_STRIPE_PUBLIC_KEY "your-stripe-public-key"

# または Netlify Web UI:
# Site settings → Build & Deploy → Environment
# から直接設定
```

### Step 4: 自動デプロイ設定

```bash
# GitHub 連携
netlify link

# これ以降、main ブランチに push すると自動デプロイ
git push origin main
# → Netlify が自動ビルド＆デプロイ
```

**結果**: `https://webdm-pro.netlify.app` で自動公開！

---

## 💳 Phase 3: 決済機能実装（Week 3-4）

### 課題：HTML単一ファイルでは Stripe webhook が難しい

**解決策**: Netlify Functions を使用（サーバーレス）

### Step 1: Netlify Functions セットアップ

```bash
# ディレクトリ構造
webdm-pro/
├── index.html          # フロントエンド（既存）
├── netlify/
│   └── functions/
│       ├── create-subscription.js   # サブスク作成
│       ├── stripe-webhook.js        # webhook 受信
│       └── generate-email.js        # Email 生成（オプション）
├── netlify.toml
├── .env
└── .gitignore
```

### Step 2: Stripe Webhook Function 実装

```javascript
// netlify/functions/stripe-webhook.js

const stripe = require('stripe')(process.env.STRIPE_SECRET_KEY)

exports.handler = async (event) => {
  // POST リクエストのみ受け付け
  if (event.httpMethod !== 'POST') {
    return {
      statusCode: 405,
      body: 'Method Not Allowed'
    }
  }

  try {
    // Stripe Signature 検証
    const sig = event.headers['stripe-signature']
    const rawBody = event.body

    const webhookSecret = process.env.STRIPE_WEBHOOK_SECRET
    const stripeEvent = stripe.webhooks.constructEvent(
      rawBody,
      sig,
      webhookSecret
    )

    // イベント処理
    switch (stripeEvent.type) {
      case 'customer.subscription.created':
        console.log('Subscription created:', stripeEvent.data.object)
        // ここでユーザーのプランを更新（localStorage など）
        break

      case 'invoice.payment_succeeded':
        console.log('Payment succeeded:', stripeEvent.data.object)
        break

      case 'customer.subscription.deleted':
        console.log('Subscription deleted:', stripeEvent.data.object)
        break
    }

    return {
      statusCode: 200,
      body: JSON.stringify({ received: true })
    }
  } catch (error) {
    console.error('Webhook error:', error.message)
    return {
      statusCode: 400,
      body: `Webhook error: ${error.message}`
    }
  }
}
```

### Step 3: Stripe 統合スクリプト（フロントエンド）

既存 HTML に追加：

```html
<!-- index.html 内に追加 -->

<script src="https://js.stripe.com/v3/"></script>

<script>
  // Stripe 初期化
  const stripe = Stripe(import.meta.env.VITE_STRIPE_PUBLIC_KEY)

  // Starter プラン購入ボタン
  async function startSubscription(planId) {
    try {
      // Netlify Function を呼び出し
      const response = await fetch('/.netlify/functions/create-subscription', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          planId: planId,
          email: userEmail // ユーザーメールアドレス
        })
      })

      const session = await response.json()

      // Stripe Checkout へリダイレクト
      const result = await stripe.redirectToCheckout({
        sessionId: session.id
      })

      if (result.error) {
        alert('エラー: ' + result.error.message)
      }
    } catch (error) {
      console.error('Subscription error:', error)
      alert('サブスク作成エラー')
    }
  }
</script>
```

---

## 🗄️ Phase 4: ユーザーデータ保存（Week 4-5）

### 課題：HTML単一ファイルではローカルストレージのみ

**現状の実装**:
```javascript
// 既存コード内
localStorage.setItem('webdm_v1.8.5', JSON.stringify(versions))
```

**制限**:
- ✅ 同一ブラウザ内では永続化
- ❌ 他のデバイスでは同期されない
- ❌ サーバーに保存されない
- ❌ プライベートブラウジングでは消える

### 改善策 1: Supabase 統合（推奨）- 無料

```html
<!-- index.html の <head> 内に追加 -->

<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>

<script>
  // Supabase 初期化
  const SUPABASE_URL = 'https://your-project.supabase.co'
  const SUPABASE_ANON_KEY = 'your-anon-key'

  const { createClient } = supabase
  const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY)

  // 既存の localStorage コードを置き換え
  async function saveVersions(versions) {
    const { data: { user }, error: authError } = await supabase.auth.getUser()
    
    if (!user) {
      // 未ログインはローカルストレージ使用
      localStorage.setItem('webdm_v1.8.5', JSON.stringify(versions))
      return
    }

    // ログイン済みはクラウドに保存
    const { error } = await supabase
      .from('user_versions')
      .upsert({
        user_id: user.id,
        versions: versions,
        updated_at: new Date()
      })

    if (error) {
      console.error('Save error:', error)
      // エラー時はローカルストレージにフォールバック
      localStorage.setItem('webdm_v1.8.5', JSON.stringify(versions))
    }
  }

  async function loadVersions() {
    const { data: { user } } = await supabase.auth.getUser()
    
    if (!user) {
      // 未ログインはローカルストレージから読み込み
      return JSON.parse(localStorage.getItem('webdm_v1.8.5') || '[]')
    }

    // ログイン済みはクラウドから読み込み
    const { data, error } = await supabase
      .from('user_versions')
      .select('versions')
      .eq('user_id', user.id)
      .single()

    if (error || !data) {
      return JSON.parse(localStorage.getItem('webdm_v1.8.5') || '[]')
    }

    return data.versions
  }
</script>
```

### Supabase テーブル設定

```sql
-- Supabase SQL エディタで実行

CREATE TABLE user_versions (
  id UUID DEFAULT uuid_generate_v4() PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  versions JSONB,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- インデックス
CREATE INDEX idx_user_versions_user_id ON user_versions(user_id);

-- Row Level Security
ALTER TABLE user_versions ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own versions"
  ON user_versions
  FOR SELECT
  USING (auth.uid() = user_id);

CREATE POLICY "Users can update own versions"
  ON user_versions
  FOR UPDATE
  USING (auth.uid() = user_id);

CREATE POLICY "Users can insert own versions"
  ON user_versions
  FOR INSERT
  WITH CHECK (auth.uid() = user_id);
```

---

## 🔑 Phase 5: API キー管理（Week 5）

### Google Gemini API キー取得（無料枠あり）

```bash
# 1. Google Cloud Console でプロジェクト作成
# https://console.cloud.google.com/

# 2. Generative AI API を有効化
# 3. API キーを生成
# 4. 月 100 万トークン無料！

# .env.local に設定
echo "VITE_GOOGLE_API_KEY=your-key-here" >> .env.local
```

### Stripe キー取得（無料で使用可）

```bash
# 1. https://dashboard.stripe.com/register
# 2. API キーを取得
# 3. .env.local に設定
echo "VITE_STRIPE_PUBLIC_KEY=pk_..." >> .env.local
echo "STRIPE_SECRET_KEY=sk_..." >> .env.local（Netlify で設定）
```

---

## 📈 完全無料のスタック構成図

```
┌─────────────────────────────────────────┐
│         ユーザー（ブラウザ）              │
└───────────────┬─────────────────────────┘
                ↓
    ┌───────────────────────────────┐
    │  Netlify ホスティング（無料）    │
    │  - index.html（既存コード）      │
    │  - CDN 配信                     │
    │  - SSL/TLS 自動化              │
    │  - 月額: ¥0                     │
    └───────────┬─────────────────────┘
                ↓
    ┌───────────────────────────────┐
    │ Netlify Functions（無料枠）     │
    │ - Stripe webhook 受信          │
    │ - メール送信処理               │
    │ - 月額: ¥0（125K呼び出し/月） │
    └───────────┬─────────────────────┘
                ↓    ↓    ↓
         ┌──────┴──┬──┴──┬──┴──────┐
         ↓         ↓     ↓         ↓
    ┌─────────┐ ┌──────┐ ┌──────┐ ┌──────┐
    │ Gemini  │ │Stripe│ │Supabase│ │GitHub│
    │  API    │ │決済  │ │  DB   │ │管理  │
    │（無料）  │ │API  │ │（無料）│ │（無料）│
    │月100M  │ │     │ │       │ │     │
    │トークン  │ │手数料│ │永続化 │ │コード │
    │無料     │ │のみ │ │同期  │ │管理 │
    └─────────┘ └──────┘ └──────┘ └──────┘
```

---

## 🎯 完全な実装ステップ

### Week 1: 基盤構築（¥0）

```
□ GitHub リポジトリ作成
  投資: ¥0
  時間: 30分

□ 既存コードを GitHub に push
  投資: ¥0
  時間: 30分

□ .env.example, README 作成
  投資: ¥0
  時間: 30分
```

### Week 2: ホスティング＆デプロイ（¥0）

```
□ Netlify サインアップ
  投資: ¥0
  時間: 10分

□ GitHub リポジトリを Netlify に接続
  投資: ¥0
  時間: 5分

□ 自動デプロイ設定確認
  投資: ¥0
  時間: 5分

□ 本番サイトが自動公開
  投資: ¥0
  時間: 10分（Netlify が自動ビルド）

結果: https://webdm-pro.netlify.app で公開！
```

### Week 3: API キー設定（¥0）

```
□ Google Gemini API キー取得
  投資: ¥0（無料枠で十分）
  時間: 30分

□ Stripe アカウント作成
  投資: ¥0（手数料は売上から）
  時間: 15分

□ Netlify 環境変数を設定
  投資: ¥0
  時間: 10分

□ テスト生成実行
  投資: ¥0
  時間: 10分
```

### Week 4: Netlify Functions 実装（¥0）

```
□ netlify/functions/ ディレクトリ作成
  投資: ¥0
  時間: 5分

□ stripe-webhook.js 実装
  投資: ¥0
  時間: 1時間

□ ローカルテスト
  投資: ¥0
  時間: 30分

□ 本番デプロイ（git push）
  投資: ¥0
  時間: 2分
```

### Week 5: Supabase 統合（¥0）

```
□ Supabase プロジェクト作成
  投資: ¥0（無料枠: 1GB）
  時間: 15分

□ テーブル作成＆RLS 設定
  投資: ¥0
  時間: 30分

□ Supabase.js クライアント統合
  投資: ¥0
  時間: 1時間

□ ローカルテスト
  投資: ¥0
  時間: 30分

□ 本番デプロイ
  投資: ¥0
  時間: 2分
```

---

## 💡 実装時の注意点

### 1. API キーの管理

```bash
# ❌ してはいけないこと
git add .env              # API キーを push しない！
git push origin .env      # 秘密キーが露出

# ✅ するべきこと
cat > .env.local << 'EOF'
VITE_GOOGLE_API_KEY=xxx
VITE_STRIPE_PUBLIC_KEY=pk_xxx
EOF

# .gitignore に追加
echo ".env.local" >> .gitignore
git add .gitignore
git commit -m "Add .env.local to gitignore"
git push

# Netlify に環境変数を設定（Web UI）
# Site settings → Build & Deploy → Environment
```

### 2. ローカル開発時のテスト

```bash
# ローカルで Netlify Functions をテスト
netlify dev

# または
npm install -D netlify-cli
npx netlify dev

# localhost:8888 でアクセス可能
# 自動的に Functions もロード
```

### 3. HTML 単一ファイルの制限と回避策

**制限**:
```javascript
// ❌ モジュール import は使えない
import { createClient } from '@supabase/supabase-js'

// ✅ CDN から読み込む
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
```

**既存コードとの互換性**:
```javascript
// 既存の generateEmail() 関数は変更不要
// Netlify Function で新規実装も可能
```

---

## 📊 初期段階での月額コスト

### Month 1-3（完全無料期間）

```
Netlify ホスティング: ¥0
  ├─ 帯域: 100GB/月（無料）
  ├─ Functions: 125,000 呼び出し（無料）
  └─ ビルド時間: 300時間/月（無料）

Google Gemini API: ¥0
  ├─ 月 100 万トークン（無料）
  └─ 使用: 月 10 万トークン程度

Stripe: ¥0
  ├─ 初期費用なし
  └─ 売上時の手数料 3.6% のみ

Supabase: ¥0
  ├─ ストレージ: 1GB（無料枠）
  ├─ API 呼び出し: 50 万/月（無料枠）
  └─ 帯域: 2GB/月（無料枠）

GitHub: ¥0（完全無料）

────────────────────────────
合計月額: ¥0
```

### Month 4+（小規模な投資が必要）

```
月 10,000 ユーザー突破時:
  - Gemini API: 月 ¥30万（100 万 tokens ÷ 月）
  - Supabase: 月 ¥5万（容量 10GB）
  - Netlify: ¥0（Function 無料枠で十分）

月額支出: ¥35万
予想売上: ¥100万（Starter 20人）
損益: +¥65万
```

---

## 🚀 デプロイメント手順（実際のコマンド）

### 1. GitHub リポジトリ初期化

```bash
cd ~/webdm-pro
git init
git config user.name "Your Name"
git config user.email "you@example.com"

# GitHub で新規リポジトリ作成後
git remote add origin https://github.com/your-username/webdm-pro.git
git branch -M main
git push -u origin main
```

### 2. Netlify デプロイ

```bash
# Netlify CLI インストール
npm install -g netlify-cli

# ログイン
netlify login

# プロジェクトをリンク
netlify init

# 以後、git push で自動デプロイ
git add .
git commit -m "Update code"
git push origin main
# → Netlify が自動的にビルド＆デプロイ
```

### 3. 環境変数設定

```bash
# Netlify Web UI で設定
# https://app.netlify.com/sites/your-site/settings/general

# または CLI で設定
netlify env:set VITE_GOOGLE_API_KEY "your-key"
netlify env:set VITE_STRIPE_PUBLIC_KEY "your-key"
netlify env:set STRIPE_SECRET_KEY "your-secret"（Netlify Functions 用）
```

---

## ✅ 最終チェックリスト

```
【Week 1】
□ GitHub リポジトリ作成
□ 既存 HTML ファイルを push
□ README.md 作成
□ .gitignore, .env.example 作成

【Week 2】
□ Netlify アカウント作成
□ GitHub と連携
□ netlify.toml 作成
□ https://webdm-pro.netlify.app で公開確認

【Week 3】
□ Google API キー取得（無料枠確認）
□ Stripe アカウント作成
□ Netlify 環境変数を設定
□ フロントエンド Stripe 統合テスト

【Week 4】
□ netlify/functions/ ディレクトリ作成
□ stripe-webhook.js 実装
□ ローカル netlify dev でテスト
□ 本番デプロイ＆テスト

【Week 5】
□ Supabase プロジェクト作成
□ テーブル＆RLS 設定
□ Supabase.js 統合
□ ローカル＆本番テスト

【完了時点】
✅ 完全無料で SaaS が稼働
✅ ユーザーはサイトを利用可能
✅ 決済機能は動作中
✅ データはクラウドに保存
✅ 自動デプロイが稼働

投資: ¥0
```

---

## 💫 初期段階での予想シナリオ

### Month 1

```
ユーザー: 100 人
AI 生成: 月 500 回
Free → Starter 転換: 0 人

費用:
  Gemini API: ¥0（無料枠内）
  Supabase: ¥0（無料枠内）
  Netlify: ¥0（無料枠内）
  
月額支出: ¥0
月額売上: ¥0
```

### Month 3

```
ユーザー: 5,000 人
AI 生成: 月 20,000 回
Free → Starter 転換: 50 人

費用:
  Gemini API: ¥5万（100 万 tokens 接近）
  Supabase: ¥0（無料枠内）
  Netlify: ¥0（無料枠内）
  
月額支出: ¥5万
月額売上: ¥25万（50人 × ¥5,000）
損益: +¥20万
```

### Month 6

```
ユーザー: 30,000 人
AI 生成: 月 150,000 回
Free → Starter 転換: 300 人

費用:
  Gemini API: ¥40万（1.5M tokens）
  Supabase: ¥5万（容量 100MB → 500MB）
  Netlify: ¥0（無料枠で十分）
  
月額支出: ¥45万
月額売上: ¥150万（300人 × ¥5,000）
損益: +¥105万（黒字化！）
```

---

## 🎁 まとめ：完全無料で SaaS を起動できる理由

### 費用ゼロの理由

```
✅ Netlify: ホスティング無料（月 100GB 帯域無料）
✅ GitHub: コード管理無料
✅ Supabase: DB 無料（月 1GB まで）
✅ Gemini: API 無料（月 100 万トークン）
✅ Stripe: 初期費用なし（手数料のみ）

→ すべてを組み合わせても初期投資 ¥0！
```

### スケール時の対応

```
Month 6 以降、月額 ¥50万程度の投資で対応可能
  - Supabase Pro: ¥25K（100GB 容量）
  - Gemini API: ¥30K（トークン増）
  - その他微調整: ¥10K

でも、月売上が ¥150万なら十分ペイ可能！
```

